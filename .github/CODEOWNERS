# Sectum AI - code ownership.
#
# Single-maintainer ownership today. The `@dmitrymaranik` handle owns every
# path; CODEOWNERS-required review in branch protection therefore routes to
# the maintainer for every PR. When a `sectum-ai/maintainers` team forms, swap
# `@dmitrymaranik` for the team slug here and keep the same `*` glob.
* @dmitrymaranik
